{
  "name": "suiteview-lambda-save-image-to-bucket",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "node --require dotenv/config -e \"console.log(require('./index').handler(require('./event.json')));\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1345.0",
    "moment-timezone": "^0.5.42",
    "request-promise-native": "^1.0.9"
  },
  "devDependencies": {
    "dotenv": "^16.0.3"
  }
}
